package org.example.pages.trade;

import org.example.utils.Page;
import org.openqa.selenium.WebDriver;

public class ConvertPage extends Page {

    public ConvertPage(WebDriver driver) {
        super(driver);
    }
}