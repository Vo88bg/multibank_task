package org.example.pages.aboutus;

import org.example.utils.Page;
import org.openqa.selenium.WebDriver;

public class AwardsPage extends Page {

    public AwardsPage(WebDriver driver) {
        super(driver);
    }
}