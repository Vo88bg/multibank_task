package org.example.pages.aboutus;

import org.example.utils.Page;
import org.openqa.selenium.WebDriver;

public class SponsorshipPage extends Page {

    public SponsorshipPage(WebDriver driver) {
        super(driver);
    }
}