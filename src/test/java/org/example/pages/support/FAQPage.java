package org.example.pages.support;

import org.example.utils.Page;
import org.openqa.selenium.WebDriver;

public class FAQPage extends Page {
    public FAQPage(WebDriver driver) {
        super(driver);
    }
}
