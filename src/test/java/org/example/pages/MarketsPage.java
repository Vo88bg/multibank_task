package org.example.pages;

import org.example.utils.Page;
import org.openqa.selenium.WebDriver;

public class MarketsPage extends Page {
    public MarketsPage(WebDriver driver) {
        super(driver);
    }
}
