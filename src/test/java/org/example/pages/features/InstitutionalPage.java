package org.example.pages.features;

import org.example.utils.Page;
import org.openqa.selenium.WebDriver;

public class InstitutionalPage extends Page {

    public InstitutionalPage(WebDriver driver) {
        super(driver);
    }
}