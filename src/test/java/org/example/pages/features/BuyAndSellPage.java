package org.example.pages.features;

import org.example.utils.Page;
import org.openqa.selenium.WebDriver;

public class BuyAndSellPage extends Page {

    public BuyAndSellPage(WebDriver driver) {
        super(driver);
    }
}